How did viewing a diff between two versions of a file help you see the bug that was introduced?

it helped me see the changes made that may have introduced the bug.

How could having easy access to the entire history of a file make you 
a more efficient programmer in the long term?

it can help me review the work progress of the file and what worked and what didn't

What do you think are the pros and cons of manually choosing when to create a commit, 
like you do in Git, vs having versions automatically saved, like Google docs does?

committing manually gets rid of extra non logical versions that can be made automatically but 
also face the danger of forgetting to commit.

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?

git is version control platform for coding which mostly have files mutually dependent 

How can you use the commands git log and git diff to view the history of files?

git log view all the commits made to the files while git diff show the changes between two selected commits